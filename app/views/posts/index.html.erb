<h1>Todo List</h1>

<% @posts.each do |post| %>
  <div class="card card-body bg-light">
    <div class="row">
      <div class="col-md-9">
        <h3><%= post.title %></h3>
        <div class="card card-body">
          <p><%= post.body %></p>
        </div>
      </div>
      <div class="col-sm-3">
        <%= link_to "lê apenas esse", post_path(post), :class => 'btn-right btn-sm btn-danger' %>
      </div>
    </div>
  </div>
<% end %>
