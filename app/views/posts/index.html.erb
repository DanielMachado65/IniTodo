<h1>Todo List</h1>

<% @posts.each do |post| %>
  <div class="card card-body bg-light">
    <div class="row">
      <div class="col-md-12">
        <h3><%= post.title %></h3>
        <div class="card card-body">
          <p><%= post.body %></p>
        </div>
        <%= link_to "lê apenas esse", post_path(post), :class => 'btn-right btn-sm btn-dark' %>
      </div>
    </div>
  </div>
<% end %>
